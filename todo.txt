multer images
dark/night mode (new content rendered will not get 'dark mode' effect, 
		 have to reapply [reclick])
page theme/skin? (profit method 2)
emotes? (profit method 3)
-fix favorites (1 account can favorite a tweet multiple times if they refresh page) - same solution as below?
-following accounts (1 account can follow another multiple times) - add the displayname to following/followers 
	[Array instead of Int, and just return .length for displaying counts, use the actual values for this issue
	& issue below]
-only be able to view tweets from accounts that you are following - same as above^